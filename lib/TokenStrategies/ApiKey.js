/**
 * API Key is a strategy to generate a token to the user, and we also store
 * it back it our database. So in the next time, we can use it as a credential
 * to authenticate.
 *
 */
class ApiKey {

    constructor(string) {

    }

    toString() {

    }

    static compare(hashAlgorithm) {
        
    }

    /**
     *
     */
    static generate(hashAlgorithm) {

    }
}
